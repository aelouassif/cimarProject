<?php
    require_once "bootstrap.php";

    include 'src/Personne.php';
    include 'src/Formation.php';
    include 'src/Evaluation.php';

    // $t = new test();
    // $t->getNote();
    // $entityManager->persist($t);
    // $entityManager->flush();

    // $t = $entityManager->find('test', 3);
    // $t->setNote("10",0);
    // $tab = $t->getNote();
    // print_r($tab);
    // $tab[0]  ="10";
    // $t->setNote($tab);
    // $entityManager->persist($t);
    // $entityManager->flush();


    // $personne1 = $entityManager->find('Personne', 15);
    // $personne2 = $entityManager->find('Personne', 17);
    // $formation = $entityManager->find('Formation', 28);
    //
    //
    //
    //
    // $evaluation1 =  $entityManager->getRepository('Evaluation')->findOneBy(array('personne' => 4,'formation' => 1));

    // $e = $evaluation1->getNoteIndividuelle();
    // $e[0] = "premier note ";
    // $evaluation1->setNoteIndividuelle($e);
    // $entityManager->persist($evaluation1);
    // $entityManager->flush();

    // print_r($evaluation1->getId());

    // $evaluation2 = new Evaluation();
    //
    // $formation->addEvaluation($evaluation1);
    // $formation->addEvaluation($evaluation2);
    //
    // $personne1->addEvaluation($evaluation1);
    //
    // $personne2->addEvaluation($evaluation2);
    // $entityManager->persist($evaluation2);
    // $entityManager->flush();
    // $formations = $entityManager->find('Formation',1);
    //
    // $personnes =  $formations->getPersonnes();

    // print_r($personne);
    // echo $personnes[1]->getId();
    // echo $evaluation[0]->getId();
    //
    //
    // $entityManager->persist($personne);
    // $entityManager->persist($formation);


    echo "fiin";

 ?>
